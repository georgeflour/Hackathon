# Web framework
fastapi
flask
uvicorn[standard]
python-multipart

# Data & validation
pydantic
pandas
python-dotenv

# Matching
rapidfuzz

# OCR 
azure-ai-documentintelligence
azure-core
azure-identity
python-dotenv

# LLM
openai

# ── Storage ───────────────────────────────────────────────────────────────────
# SQLite is built into Python — no extra package needed.
# FAISS (vector search for RAG knowledge corpus)
faiss-cpu
# Sentence embeddings
sentence-transformers>=3.0

# Testing
pytest
requests

# Additional libraries for FastAPI and Flask applications
torch
transformers
bitsandbytes
accelerate
peft
